### Keeping template and solutions in sync

# pull latest bug-fixes / utilities from the public template
git fetch upstream
git merge upstream/main    # or git rebase upstream/main

# push my answers (private)
git push
